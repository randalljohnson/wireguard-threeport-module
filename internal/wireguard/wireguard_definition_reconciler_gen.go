// generated by 'threeport-sdk gen' - do not edit

package wireguard

import (
	"errors"
	"fmt"
	api_v0 "github.com/randalljohnson/wireguard-threeport-module/pkg/api/v0"
	client_v0 "github.com/randalljohnson/wireguard-threeport-module/pkg/client/v0"
	tpapi_lib "github.com/threeport/threeport/pkg/api/lib/v0"
	tpapi_v0 "github.com/threeport/threeport/pkg/api/v0"
	tpclient_lib "github.com/threeport/threeport/pkg/client/lib/v0"
	controller "github.com/threeport/threeport/pkg/controller/v0"
	event "github.com/threeport/threeport/pkg/event/v0"
	notifications "github.com/threeport/threeport/pkg/notifications/v0"
	util "github.com/threeport/threeport/pkg/util/v0"
	"os"
	"os/signal"
	"strings"
	"syscall"
	"time"
)

// WireguardDefinitionReconciler reconciles system state when a WireguardDefinition
// is created, updated or deleted.
func WireguardDefinitionReconciler(r *controller.Reconciler) {
	r.ShutdownWait.Add(1)
	reconcilerLog := r.Log.WithValues("reconcilerName", r.Name)
	reconcilerLog.Info("reconciler started")
	shutdown := false

	// create a channel to receive OS signals
	osSignals := make(chan os.Signal, 1)
	lockReleased := make(chan bool, 1)

	// register the os signals channel to receive SIGINT and SIGTERM signals
	signal.Notify(osSignals, syscall.SIGINT, syscall.SIGTERM)

	for {
		// create a fresh log object per reconciliation loop so we don't
		// accumulate values across multiple loops
		log := r.Log.WithValues("reconcilerName", r.Name)

		if shutdown {
			break
		}

		// check for shutdown instruction
		select {
		case <-r.Shutdown:
			shutdown = true
		default:
			// pull message off queue
			msg := r.PullMessage()
			if msg == nil {
				continue
			}

			// consume message data to capture notification from API
			notif, err := notifications.ConsumeMessage(msg.Data)
			if err != nil {
				log.Error(
					err, "failed to consume message data from NATS",
					"msgData", string(msg.Data),
				)
				r.RequeueRaw(msg)
				log.V(1).Info("wireguard definition reconciliation requeued with identical payload and fixed delay")
				continue
			}

			// determine the correct object version from the notification
			var wireguardDefinition tpapi_lib.ReconciledThreeportApiObject
			switch notif.ObjectVersion {
			case "v0":
				wireguardDefinition = &api_v0.WireguardDefinition{}
			default:
				log.Error(errors.New("received unrecognized version of wireguard definition object"), "")
				r.RequeueRaw(msg)
				log.V(1).Info("wireguard definition reconciliation requeued with identical payload and fixed delay")
				continue
			}

			// decode the object that was sent in the notification
			if err := wireguardDefinition.DecodeNotifObject(notif.Object); err != nil {
				log.Error(err, "failed to marshal object map from consumed notification message")
				r.RequeueRaw(msg)
				log.V(1).Info("wireguard definition reconciliation requeued with identical payload and fixed delay")
				continue
			}
			log = log.WithValues("wireguardDefinitionID", wireguardDefinition.GetId())

			// back off the requeue delay as needed
			requeueDelay := controller.SetRequeueDelay(
				notif.CreationTime,
			)

			// check for lock on object
			locked, ok := r.CheckLock(wireguardDefinition)
			if locked || ok == false {
				r.Requeue(wireguardDefinition, requeueDelay, msg)
				log.V(1).Info("wireguard definition reconciliation requeued")
				continue
			}

			// set up handler to unlock and requeue on termination signal
			go func() {
				select {
				case <-osSignals:
					log.V(1).Info("received termination signal, performing unlock and requeue of wireguard definition")
					r.UnlockAndRequeue(wireguardDefinition, requeueDelay, lockReleased, msg)
				case <-lockReleased:
					log.V(1).Info("reached end of reconcile loop for wireguard definition, closing out signal handler")
				}
			}()

			// put a lock on the reconciliation of the created object
			if ok := r.Lock(wireguardDefinition); !ok {
				r.Requeue(wireguardDefinition, requeueDelay, msg)
				log.V(1).Info("wireguard definition reconciliation requeued")
				continue
			}

			// retrieve latest version of object
			var latestWireguardDefinition tpapi_lib.ReconciledThreeportApiObject
			var getLatestErr error
			switch notif.ObjectVersion {
			case "v0":
				latestObject, err := client_v0.GetWireguardDefinitionByID(
					r.APIClient,
					r.APIServer,
					wireguardDefinition.GetId(),
				)
				latestWireguardDefinition = latestObject
				getLatestErr = err
			default:
				getLatestErr = errors.New("received unrecognized version of wireguard definition object")
			}

			// check if error is 404 - if object no longer exists, no need to requeue
			if errors.Is(getLatestErr, tpclient_lib.ErrObjectNotFound) {
				log.Info("object no longer exists - halting reconciliation")
				r.ReleaseLock(wireguardDefinition, lockReleased, msg, true)
				continue
			}
			if getLatestErr != nil {
				log.Error(getLatestErr, "failed to get wireguard definition by ID from API")
				r.UnlockAndRequeue(wireguardDefinition, requeueDelay, lockReleased, msg)
				continue
			}
			wireguardDefinition = latestWireguardDefinition

			// determine which operation and act accordingly
			switch notif.Operation {
			case notifications.NotificationOperationCreated:
				if wireguardDefinition.ScheduledForDeletion() != nil {
					log.Info("wireguard definition scheduled for deletion - skipping create")
					break
				}
				var operationErr error
				var customRequeueDelay int64
				switch wireguardDefinition.GetVersion() {
				case "v0":
					requeueDelay, err := v0WireguardDefinitionCreated(
						r,
						wireguardDefinition.(*api_v0.WireguardDefinition),
						&log,
					)
					customRequeueDelay = requeueDelay
					operationErr = err
				default:
					operationErr = errors.New("unrecognized version of wireguard definition encountered for creation")
				}
				if operationErr != nil {
					errorMsg := "failed to reconcile created wireguard definition object"
					log.Error(operationErr, errorMsg)
					r.EventsRecorder.HandleEventOverride(
						&tpapi_v0.Event{
							Note:   util.Ptr(errorMsg),
							Reason: util.Ptr(event.ReasonFailedCreate),
							Type:   util.Ptr(event.TypeNormal),
						},
						wireguardDefinition.GetId(),
						wireguardDefinition.GetVersion(),
						wireguardDefinition.GetType(),
						operationErr,
						&log,
					)
					r.UnlockAndRequeue(
						wireguardDefinition,
						requeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				if customRequeueDelay != 0 {
					log.Info("create requeued for future reconciliation")
					r.UnlockAndRequeue(
						wireguardDefinition,
						customRequeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
			case notifications.NotificationOperationUpdated:
				var operationErr error
				var customRequeueDelay int64
				switch wireguardDefinition.GetVersion() {
				case "v0":
					requeueDelay, err := v0WireguardDefinitionUpdated(
						r,
						wireguardDefinition.(*api_v0.WireguardDefinition),
						&log,
					)
					customRequeueDelay = requeueDelay
					operationErr = err
				default:
					operationErr = errors.New("unrecognized version of wireguard definition encountered for creation")
				}
				if operationErr != nil {
					errorMsg := "failed to reconcile updated wireguard definition object"
					log.Error(operationErr, errorMsg)
					r.EventsRecorder.HandleEventOverride(
						&tpapi_v0.Event{
							Note:   util.Ptr(errorMsg),
							Reason: util.Ptr(event.ReasonFailedUpdate),
							Type:   util.Ptr(event.TypeNormal),
						},
						wireguardDefinition.GetId(),
						wireguardDefinition.GetVersion(),
						wireguardDefinition.GetType(),
						operationErr,
						&log,
					)
					r.UnlockAndRequeue(
						wireguardDefinition,
						requeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				if customRequeueDelay != 0 {
					log.Info("update requeued for future reconciliation")
					r.UnlockAndRequeue(
						wireguardDefinition,
						customRequeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
			case notifications.NotificationOperationDeleted:
				var operationErr error
				var customRequeueDelay int64
				switch wireguardDefinition.GetVersion() {
				case "v0":
					requeueDelay, err := v0WireguardDefinitionDeleted(
						r,
						wireguardDefinition.(*api_v0.WireguardDefinition),
						&log,
					)
					customRequeueDelay = requeueDelay
					operationErr = err
				default:
					operationErr = errors.New("unrecognized version of wireguard definition encountered for creation")
				}
				if operationErr != nil {
					errorMsg := "failed to reconcile deleted wireguard definition object"
					log.Error(operationErr, errorMsg)
					r.EventsRecorder.HandleEventOverride(
						&tpapi_v0.Event{
							Note:   util.Ptr(errorMsg),
							Reason: util.Ptr(event.ReasonFailedDelete),
							Type:   util.Ptr(event.TypeNormal),
						},
						wireguardDefinition.GetId(),
						wireguardDefinition.GetVersion(),
						wireguardDefinition.GetType(),
						operationErr,
						&log,
					)
					r.UnlockAndRequeue(
						wireguardDefinition,
						requeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				if customRequeueDelay != 0 {
					log.Info("delete requeued for future reconciliation")
					r.UnlockAndRequeue(
						wireguardDefinition,
						customRequeueDelay,
						lockReleased,
						msg,
					)
					continue
				}
				deletionTimestamp := util.Ptr(time.Now().UTC())
				deletedWireguardDefinition := api_v0.WireguardDefinition{
					Common: tpapi_v0.Common{ID: util.Ptr(wireguardDefinition.GetId())},
					Reconciliation: tpapi_v0.Reconciliation{
						DeletionAcknowledged: deletionTimestamp,
						DeletionConfirmed:    deletionTimestamp,
						Reconciled:           util.Ptr(true),
					},
				}
				_, err = client_v0.UpdateWireguardDefinition(
					r.APIClient,
					r.APIServer,
					&deletedWireguardDefinition,
				)
				if err != nil {
					log.Error(err, "failed to update wireguard definition to mark as deleted")
					r.UnlockAndRequeue(wireguardDefinition, requeueDelay, lockReleased, msg)
					continue
				}
				_, err = client_v0.DeleteWireguardDefinition(
					r.APIClient,
					r.APIServer,
					wireguardDefinition.GetId(),
				)
				if err != nil {
					log.Error(err, "failed to delete wireguard definition")
					r.UnlockAndRequeue(wireguardDefinition, requeueDelay, lockReleased, msg)
					continue
				}
			default:
				log.Error(
					errors.New("unrecognized notifcation operation"),
					"notification included an invalid operation",
				)
				r.UnlockAndRequeue(
					wireguardDefinition,
					requeueDelay,
					lockReleased,
					msg,
				)
				continue
			}

			// set the object's Reconciled field to true if not deleted
			if notif.Operation != notifications.NotificationOperationDeleted {
				reconciledWireguardDefinition := api_v0.WireguardDefinition{
					Common:         tpapi_v0.Common{ID: util.Ptr(wireguardDefinition.GetId())},
					Reconciliation: tpapi_v0.Reconciliation{Reconciled: util.Ptr(true)},
				}
				updatedWireguardDefinition, err := client_v0.UpdateWireguardDefinition(
					r.APIClient,
					r.APIServer,
					&reconciledWireguardDefinition,
				)
				if err != nil {
					log.Error(err, "failed to update wireguard definition to mark as reconciled")
					r.UnlockAndRequeue(wireguardDefinition, requeueDelay, lockReleased, msg)
					continue
				}
				log.V(1).Info(
					"wireguard definition marked as reconciled in API",
					"wireguard definitionName", updatedWireguardDefinition.Name,
				)
			}

			// release the lock on the reconciliation of the created object
			if ok := r.ReleaseLock(wireguardDefinition, lockReleased, msg, true); !ok {
				log.Error(errors.New("wireguard definition remains locked - will unlock when TTL expires"), "")
			} else {
				log.V(1).Info("wireguard definition unlocked")
			}

			// log and record event for successful reconciliation
			successMsg := fmt.Sprintf(
				"wireguard definition successfully reconciled for %s operation",
				strings.ToLower(string(notif.Operation)),
			)
			if err := r.EventsRecorder.RecordEvent(
				&tpapi_v0.Event{
					Note:   util.Ptr(successMsg),
					Reason: util.Ptr(event.GetSuccessReasonForOperation(notif.Operation)),
					Type:   util.Ptr(event.TypeNormal),
				},
				wireguardDefinition.GetId(),
				wireguardDefinition.GetVersion(),
				wireguardDefinition.GetType(),
			); err != nil {
				log.Error(err, "failed to record event for successful wireguard definition reconciliation")
			}
			log.Info(successMsg)
		}
	}

	r.Sub.Unsubscribe()
	reconcilerLog.Info("reconciler shutting down")
	r.ShutdownWait.Done()
}
